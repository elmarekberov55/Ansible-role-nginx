- name: install nginx
  apt:
    update_cache: yes
    name: nginx
    state: latest
  become: yes
  notify: Restart Nginx

- name: start nginx
  service:
    name: nginx
    state: started
    enabled: yes
  notify: Restart Nginx
